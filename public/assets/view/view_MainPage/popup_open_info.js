let comp_Id = competitions.map(c => c.comp_id);
let competition_Name = competitions.map(c => c.competition_name);
let coUntry = competitions.map(c => c.country);
let cIty = competitions.map(c => c.city);
let start_Date = competitions.map(c => c.start_date);
let end_Date = competitions.map(c => c.end_date);
let diVision = competitions.map(c => c.division);
let age_Group = competitions.map(c => c.age_group);
let sEx = competitions.map(c => c.sex);
let tYpe = competitions.map(c => c.type);
let categorIes = competitions.map(c => c.categories);
let numberOfcomp = comp_Id.length;